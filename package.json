{
  "name": "leathercraft-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\""
  },
  "workspaces": ["frontend", "backend"],
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
